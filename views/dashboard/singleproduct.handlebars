<h1>Hello </h1>
<div class="card">
    <div class="row">
        <div class="col l3">
            <div class="card-content">
                <img src="{{product_image}}" class="card-image" />
                <span class="card-title">
                    {{product_name}}
                </span>
            </div>
        </div>
   </div>
</div>